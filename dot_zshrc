 #>>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/Users/tariktornes/miniconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/Users/tariktornes/miniconda3/etc/profile.d/conda.sh" ]; then
        . "/Users/tariktornes/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/Users/tariktornes/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<


# setting up ghcup env (Haskell)
[ -f "/Users/tariktornes/.ghcup/env" ] && source "/Users/tariktornes/.ghcup/env" 



#########################################################################
#
# MY CONFIGURATIONS
#
#########################################################################

# --------------------------------------------------
# VIM KEYBINDING 
# --------------------------------------------------
bindkey -v
bindkey -M viins 'jk' vi-cmd-mode

# zsh's vi-backward-delete-char (default backspace in viins) won't delete
# past where insert mode began. Rebind so "i" from normal mode gives back
# full emacs-style backspace to the start of the line.
bindkey -M viins '^?' backward-delete-char
bindkey -M viins '^H' backward-delete-char



# --------------------------------------------------
#  SHELL PROMPT
# --------------------------------------------------


function get_env_name {
    if [[ -n "$CONDA_DEFAULT_ENV" ]]; then
        # If a Conda environment is active
        echo "($CONDA_DEFAULT_ENV) "
    elif [[ -n "$VIRTUAL_ENV" ]]; then
        # If a virtualenv environment is active
        echo "[$(basename "$VIRTUAL_ENV")] "
    fi
}



function zle-keymap-select {

    env_name=$(get_env_name)
    
    
    if [[ ${KEYMAP} == vicmd ]]; then
        # Command mode: RED
        PROMPT="${env_name}%n@Tariks-Mac %1~ %F{red}❮ %f"
    else
        # Insert mode: GREEN
        PROMPT="${env_name}%n@Tariks-Mac %1~ %F{green}❯ %f"
    fi
    zle reset-prompt
}

zle -N zle-keymap-select

# Also trigger the function on every mode switch
function zle-line-init {
    zle -K viins    # start in insert mode
    zle-keymap-select
}
zle -N zle-line-init

# Makes sure that default is insert mode
PROMPT="${get_env_name}%n@Tariks-Mac %1~ %F{green}❯ %f"



# --------------------------------------------------
#  COLOR CONFIG
# --------------------------------------------------

export CLICOLOR=1
export LSCOLORS=DxcxcxdxBxegedabagacad



# --------------------------------------------------
#  ALIASES
# --------------------------------------------------

alias ta='tmux attach'
alias fvim='nvim $(fzf --preview="cat {}")'
alias ls='ls -G'
alias ll='ls -lG'
alias la='ls -aG'
alias conf='cd ~/.config/'
alias csem='cd ~/Documents/UniLeuven/semester2/'
alias wisecow='fortune | cowsay -n'
alias brave='open -a "Brave Browser"'
alias braven='open -na "Brave Browser"'
alias moodle='open -a "Brave Browser" https://moodle.uni.lu/my/index.php' 
alias ghub='open -a "Brave Browser" https://github.com/TarikTornes/'
alias skim='/Applications/Skim.app/Contents/MacOS/Skim'
alias snap='tmutil snapshot'
alias nxt='open http://raspitarik:8080/apps/files/files'
alias pyocdflash='pyocd flash --pack ~/Downloads/NordicSemiconductor.nRF_DeviceFamilyPack.8.44.1.pack --target nrf5340_xxaa build/merged.hex'


export PATH="/opt/homebrew/opt/node@22/bin:$PATH"


fcf() {
  local target
  target=$(fzf) || return
  if [ -d "$target" ]; then
    cd "$target"
  else
    cd "$(dirname "$target")"
  fi
}



# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/tariktornes/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/tariktornes/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/tariktornes/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/tariktornes/google-cloud-sdk/completion.zsh.inc'; fi
export PATH="$PATH:/Users/tariktornes/google-cloud-sdk/bin"

# Created by `pipx` on 2026-03-03 15:59:58
export PATH="$PATH:/Users/tariktornes/.local/bin"
